<%= render 'shared/header' %>
<%= stylesheet_link_tag "top" %>
<%= stylesheet_link_tag "play" %>

<div class="content_wrap">
  <h1>遊び</h1>
     <h5>毎日24時間、先着1人だけ配信できます。</h5>
     <h5>1人6時間までです。</h5>
     <h5>画面全部使うのでバナー広告</h5>
     <h5>Twitterでゲームやります！この画面内で謎を解いてアイテムを発見すると1000円プレゼントします！</h5>
     <p>同時接続数：12355人</p>

  <div class="live_wrap">
    <div class="video">
    </div>
  </div>

<div class="comments_wrap">
    <% @plays.each do |play|%>
        <div class="form-group row"><%= play.message %></div>
    <% end %>
</div>

 <%= form_with(url: "/play/", local: true, html:{ class:"edit-form" ,id:"edit-form"}) do |form| %>

      <div class="form-group row">
          <div class="col-md-12 p-8">
              <input id="name" type="txt" class="input form-control" placeholder="メッセージを送信" name="message" value="" required autocomplete="name">
          </div>
      </div>

      <div class="form-group mb-0">
          <button type="submit" class="btn btn-primary">
          投稿する
          </button>
      </div>
  <% end %>
</div>